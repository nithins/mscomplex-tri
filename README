/******mscomplex 2d grid software**********/

code to compute ms complex for 2d rect grids

/**********build instructions *************/

to build you need

CMake
boost(program_options thread serialization)

OpenCL (+)

QT(Opengl gui xml) (*)
OpenGL (*)
QGLViewer (*)
Glut or FTGL (*)
Python (*)

* needed only for viewing component .. could be
made optional later

+ needed for massively parallel computation..
can do without out .. may be made optional later. 

/**********Run options      ****************/

type "mscomplex-2d -h " for available options